#prompt for a sequence of floats or ints, the last num being zero
#   "Enter a Value (zero to quit): "    <-- prompt x amount of times until user enters 0
#assume at least one num will be entered
#when zero is entered, display the count, sum, average, min, and max of the sequence
#   "Count: " <-- should be an int
#   "Sum: " <--should be a float
#   "Average: " <-- should be a float
#   "Minimum: "
#   "Maximum: "
#DO NOT store values in a list or use min(), max(), or mean() functions

#example sequence: 5, 6, 11, 0
# count = 3; sum = 22.0; average = 7.3333333333333; min = 5.0; max = 11.0
